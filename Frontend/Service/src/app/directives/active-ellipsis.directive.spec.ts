import { ActiveEllipsisDirective } from './active-ellipsis.directive';

describe('ActiveEllipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveEllipsisDirective();
    expect(directive).toBeTruthy();
  });
});